<h3 class="center-align">Account</h3>


<table class="striped">
    <thead>
        <tr>
            <th></th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>Username</td>
            <td>{{user.username}}</td>
        </tr>
        <tr>
            <td>Email</td>
            <td>{{user.email}}</td>
        </tr>
        <tr>
            <td>Native Language</td>
            <td>{{user.native_language}}</td>
        </tr>
        <tr>
            <td>Target Language</td>
            <td>{{user.target_language}}</td>
        </tr>
    </tbody>
</table>

<div class="row">
    {{!-- make a button to act like a link --}}
    <button onclick="window.location.href='/account/edit/{{user.id}}';" type="button" class="update-btn btn waves-effect waves-light blue darken-4 col s12">Update Info</button>


</div>